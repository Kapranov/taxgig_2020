defmodule ServerWeb.GraphQL.Resolvers.Services.PlaidResolver do
  @moduledoc """
  The Plaid GraphQL resolvers.
  """

  def list_account(%{}, %{}, %{}) do
  end

  def list_transaction(%{}, %{}, %{}) do
  end

  def show_account(%{}, %{}, %{}) do
  end

  def show_transaction(%{}, %{}, %{}) do
  end

  def create(%{}, %{}, %{}) do
  end

  def delete_account(%{}, %{}, %{}) do
  end

  def delete_transaction(%{}, %{}, %{}) do
  end
end
